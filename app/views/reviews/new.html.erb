<%= simple_form_for [@restaurant, @review] do |f| %>
  <h1 class="lead">New review for "<%= @restaurant.name %>"" </h1>
  <hr>
  <%= f.input :rating, as: :select, collection: [1, 2, 3, 4, 5] %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
